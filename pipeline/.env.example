# Future API keys — not needed for the yfinance provider
# POLYGON_API_KEY=
# ALPHA_VANTAGE_API_KEY=

# Override where snapshots are written. Defaults to ../News/data/options
# (relative to the Options Dashboard folder)
# OPTIONS_OUTPUT_DIR=
